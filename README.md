# Stock-Price-Prediction
A simple stock price prediction pipeline using Python, pandas, matplotlib and scikit-learn. This project loads historical stock data from a CSV, engineers features (moving averages, returns, volatility), trains a linear regression model, and visualizes predicted vs. actual closing prices.
